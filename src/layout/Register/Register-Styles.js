import styled, { css } from 'styled-components';

export const RegisterPage = styled.section`
  ${({ theme }) => css`
      width: 100vw;
      height: 100vh;

      >p{
        color: white
      }
    }
  `}
`;
