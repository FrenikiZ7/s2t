import styled, { css } from 'styled-components';

export const BannerSlideElement = styled.div`
  ${({ theme }) => css`

  margin: 0 auto;
      .swiper-wrapper {
        height: 500px;
        width: 300px;
      }
  `}
`;
